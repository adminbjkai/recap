# Tasks

> Product direction and ordered slices:
> [docs/product_roadmap.md](docs/product_roadmap.md). UX inspiration
> sources and the borrow-patterns-not-code rule:
> [docs/ux_inspiration.md](docs/ux_inspiration.md).

## Phase 1: Reliable Core

- [x] Create a job model and working-directory convention with `job.json`
- [x] Implement ingest to accept a source recording and persist `original.ext`
- [x] Add metadata extraction via `ffprobe` and persist `metadata.json`
- [x] Add FFmpeg normalization to produce `analysis.mp4`
- [x] Add FFmpeg audio extraction to produce `audio.wav` as 16kHz mono WAV
- [x] Integrate faster-whisper as the default transcription engine
- [ ] Support WhisperX as an optional precision path when word-level timing is needed
- [x] Persist transcript outputs to `transcript.json` and `transcript.srt`
- [x] Implement basic Markdown report generation as `report.md`
- [x] Make every Phase 1 step restartable from existing artifacts
- [x] Add basic job status updates to reflect stage progress and failures
- [x] Validate the Phase 1 flow on a sample recording end to end

## Phase 2: Smart Visuals v1

- [x] Integrate PySceneDetect and write `scenes.json` (opt-in via `recap scenes`)
- [x] Extract one representative frame per scene into `candidate_frames/` (with single-scene fallback when no cuts)
- [x] Add pHash-based duplicate marking (`frame_scores.json`, opt-in via `recap dedupe`)
- [x] Add SSIM checks for borderline duplicate frames (folded into `recap dedupe`; extends `frame_scores.json`)
- [x] Integrate Tesseract OCR for text extraction and novelty scoring (folded into `recap dedupe`; extends `frame_scores.json` with per-frame `ocr_text` and `text_novelty`)

## Phase 3: Semantic Alignment

- [ ] Implement full chapter proposal logic using transcript shifts, pauses, speaker changes, and scene boundaries
  - [x] Pause + speaker-change + scene-boundary fusion shipped (opt-in via `recap chapters`; reads `transcript.json` and, when present, `scenes.json`; writes `chapter_candidates.json` with `source_signal` in {"pauses","pauses+speakers","pauses+scenes","pauses+speakers+scenes"}, emits pre-merge `speaker_change_count` in speaker-aware mode and `scenes_source` plus pre-merge `scene_change_count` in scenes-aware mode). Topic-shift detection, speaker recognition/labeling, and chapter titling remain deferred.
- [x] Persist chapter proposals to `chapter_candidates.json` (pause-only when neither speakers nor a usable `scenes.json` are available; fuses speaker-change boundaries when Deepgram utterances are present and scene-cut boundaries when `scenes.json` is present and not a fallback; topic-shift detection, speaker recognition/labeling, and chapter titling remain deferred)
- [x] Add transcript windowing around frame timestamps using a fuzzy plus or minus 5 to 7 second range (opt-in via `recap window`; writes `frame_windows.json`)
- [x] Integrate OpenCLIP scoring between candidate frames and transcript chunks (opt-in via `recap similarity`)
- [x] Rank frames per chapter using deduplication, OCR novelty, and semantic similarity together (opt-in via `recap rank`; writes `frame_ranks.json`)
- [x] Apply deterministic screenshot keep/reject rules before shortlist finalization (opt-in via `recap shortlist`; writes `frame_shortlist.json`; `selected_frames.json` remains reserved for Phase 4 post-VLM finalists; blur/VLM-dependent visual-quality rules remain deferred).
- [x] Add optional Deepgram cloud transcription engine with diarized utterances (opt-in via `--engine deepgram` on `recap run` and `recap transcribe`; `DEEPGRAM_API_KEY` env required only on recompute; adds `utterances`, `speakers`, `words`, `provider_metadata` as additive optional fields on `transcript.json`; `faster-whisper` remains the default; Groq, WhisperX, pyannote, and speaker recognition/manual labels remain deferred).

## Phase 4: Precision Polish

- [x] Add optional VLM verification for only the top 1 to 3 frames per chapter (opt-in via `recap verify`; reads `frame_shortlist.json` and only verifies frames with `decision in {"hero","supporting"}`; mock + Gemini providers via stdlib only; `recap run` remains Phase-1-only)
- [x] Pass exact transcript context into finalist verification (per-frame `window_text` from `frame_windows.json` and per-chapter `text` from `chapter_candidates.json`, truncated to `WINDOW_CONTEXT_CHARS = 1500` / `CHAPTER_CONTEXT_CHARS = 1500`)
- [x] Support tie-breaking and caption generation during VLM verification (hero promotion of the highest-ranked surviving supporting when the original hero is VLM-rejected, tagged `vlm_tie_broken_by_rank`; captions returned by Gemini are stored on `verification.caption` truncated to `VLM_MAX_CAPTION_CHARS = 240`; mock provider never captions and keeps `caption_mode = "off"`)
- [x] Persist final frame selections to `selected_frames.json`
- [x] Improve Markdown assembly to embed finalized screenshots and captions (opt-in via presence of `selected_frames.json`; `recap assemble` reads `selected_frames.json` and `chapter_candidates.json` and inserts a `## Chapters` section between `## Media` and `## Transcript` with hero/supporting images and VLM-provided captions, atomic write via `report.md.tmp`; absent `selected_frames.json` → report is byte-identical to the Phase-1 basic output; no VLM calls; no chapter titling; no new CLI flag; `recap run` remains Phase-1-only)
- [x] Add optional HTML export to `report.html` (opt-in via `recap export-html --job <path> [--force]`; reads the same artifacts as `recap assemble` and writes a standalone `report.html` with inline CSS; when `selected_frames.json` is present embeds hero/supporting images and captions via relative `candidate_frames/<frame_file>` paths; no new dependencies, no Markdown parsing, no network, no VLM/LLM calls; `recap run` remains Phase-1-only)
- [x] Add optional DOCX export to `report.docx` (opt-in via `recap export-docx --job <path> [--force]`; uses `python-docx>=1.1`; reads the same artifacts as `recap export-html` and embeds hero/supporting screenshots via `Document.add_picture` at a fixed 6.0-inch width; captions rendered as italic runs only when `verification.caption` is a non-empty string; when `selected_frames.json` is absent the document still renders header/media/transcript with no Chapters section; determinism caveat: DOCX package metadata is timestamped, so reruns are not byte-identical; `recap run` remains Phase-1-only; PDF and Notion export remain deferred)

## UI

- [x] Add read-only local dashboard for existing jobs and artifacts (`recap ui --host 127.0.0.1 --port 8765 --jobs-root jobs`; stdlib `http.server.ThreadingHTTPServer` only, no new dependencies; reads `jobs/<id>/job.json` and serves a jobs index, a per-job detail page, and a small whitelist of artifacts including `report.md`/`report.html`/`report.docx` and `candidate_frames/*.{jpg,jpeg,png}`; path-traversal hardened; 127.0.0.1-only by default; no POST routes, no subprocess calls, no stage execution)
- [x] Expand job detail page with Errors surface and Chapters & selected-frames thumbnail summary (read-only)
- [x] Add POST surface to rerun assemble/export-html/export-docx from the dashboard (CSRF token generated at server startup; Host header pinned; 4 KiB body cap; 60 s subprocess timeout; stdout/stderr truncated to 8 KiB UTF-8; per-job threading lock with 2 s acquire timeout returning 429; results cached in-memory and shown at `/job/<id>/run/<stage>/last`; `recap run` and every other stage remain CLI-only)
- [x] Start a new `recap run` from the browser via `/new` + `POST /run` (new `--sources-root` flag on `recap ui`, default `sample_videos`; extension whitelist `.mp4/.mov/.mkv/.webm/.m4v`; synchronous `recap ingest` inside the handler; background daemon thread runs the full `recap run` via `subprocess.Popen` with a 1-hour timeout; global `threading.Semaphore(1)` caps concurrent runs; in-memory result cache keyed `(job_id, "run")` visible at `/job/<id>/run/run/last`; `run` stage is read-only via the last-result route and is NOT in `_RUNNABLE_STAGES`; detail page shows a "Run in progress" banner plus a 10-second meta refresh when any stage is running)
- [x] Let the `/new` form pick a transcription engine (faster-whisper default; deepgram opt-in when `DEEPGRAM_API_KEY` is in the server environment. No key-entry UI; key is read only from the shell that launched `recap ui`; server-side allowlist `{faster-whisper, deepgram}`; 400 with inline error on invalid engine; 400 with inline error on `engine=deepgram` without the env var; `--engine <value>` forwarded into background `recap run`; subprocess inherits the server environment so the key reaches the child without UI involvement)
- [x] Add "Generate rich report" dashboard button that runs the 11-stage chain (scenes→dedupe→window→similarity→chapters→rank→shortlist→verify mock→assemble →export-html→export-docx) in a background thread (mock VLM only; shares the `_run_slot` semaphore with `recap run`; per-job lock ownership is explicitly transferred to the background worker; 1-hour chain budget; captured stdout/stderr truncated to 8 KiB per stage; skip-aware stages short-circuit on reruns; `recap run` composition unchanged)
- [x] Accept a browser recording upload for the source video (browser Screen Capture + `MediaRecorder` in the React `/app/new` "Record screen" tab; new `POST /api/recordings` streams the raw `video/webm` or `video/mp4` body into `<sources-root>/recording-<UTC>-<hex>.<ext>` under a server-picked filename, with a 2 GiB Content-Length cap that rejects before reading, 256 KiB streaming chunks, Host pinning, `X-Recap-Token` CSRF, and a belt-and-braces running size cap; browser-supplied filename is ignored; the upload appears in `GET /api/sources` automatically so `POST /api/jobs/start` can consume it with the existing `sources-root` source kind; save and start are separate explicit steps; browsers without `getDisplayMedia`/`MediaRecorder` render an unsupported notice; `scripts/verify_api.py` grows with five recording cases; Vitest `RecordingPanel` suite covers unsupported/ready/recording/preview/save/error states; no new Python runtime deps; `recap/job.py STAGES` and `recap run` composition unchanged; legacy `/new` + `POST /run` remain live as fallbacks)
- [ ] Browser file upload for pre-existing local video files (drag-drop / file picker, separate from live recording)
- [x] Render a timestamped transcript viewer with optional speaker rows (`GET /job/<id>/transcript`; prefers `utterances[]` with integer or non-empty string speaker ids, renders `Speaker {n}` in a dedicated column; falls back to `segments[]` with no Speaker column; filters rows with empty text; missing or malformed `transcript.json` renders a 200 empty/error state with a single `[recap-ui] transcript skipped: ...` log line; detail page links to the viewer only when `transcript.json` exists; no JavaScript, no video player, no backend changes)
- [x] Inline video player with transcript-row jump links (whitelists `analysis.mp4` + `video/mp4`; adds a Range-aware static handler that streams in 64 KiB chunks, supports `bytes=a-b`, `bytes=a-`, `bytes=-n` single-range forms, returns 416 on unsatisfiable and 200 full body on malformed Range; `/job/<id>/transcript` renders a `<video id="player" controls preload="metadata">` only when `analysis.mp4` exists and rewrites Time cells into `<button class="ts" data-start="{float}">` + a ~10-line inline script that sets `player.currentTime`; no multi-range support, no active-row highlighting, no speaker-isolated tracks; localhost-only)
- [x] Active-row highlighting and auto-scroll as the video plays (inline JS extends the existing transcript-page script; builds an ascending-sorted row index from per-row `data-start` attributes, toggles a `tr.active` class on `timeupdate` / `seeking` / `play`, and calls `scrollIntoView({block:'nearest',behavior:'smooth'})`; `wheel`/`touchmove`/`scroll`/arrow-key listeners set a `lastUserScroll` timestamp and suspend auto-scroll for 3 s so the page doesn't fight the user; active row styled with a soft background + 3 px left accent so the cue isn't color-only; no-video fallback unchanged — no `<tr data-start>`, no buttons, no sync script)
- [x] Speaker-colored transcript rows (pure CSS + server-side classnames; only active when the transcript data source is `utterances[]` with at least one valid speaker id; stable first-seen `speaker-0`..`speaker-7` classes cycle modulo `_SPEAKER_PALETTE_SIZE = 8`; small `<p class="speakers-legend">` above the table lists each distinct speaker with its color swatch; speaker-tint rules placed before `tr.active` so active-row yellow still overrides during playback; segments-only transcripts render unchanged)
- [ ] Speaker-isolated audio / per-speaker navigation
- [ ] Cancel a running job from the browser
- [ ] Persist `/job/<id>/run/<stage>/last` history across server restarts
- [ ] Start or rerun pipeline stages from the UI
- [x] Job library organization — rename, project/folder, archive (new `<jobs-root>/.recap_library.json` sidecar maps `{job_id: {title, project, archived}}` with atomic `<file>.tmp` → `os.replace` under a process-wide `_library_lock`; **no directory moves** — the sidecar captures organization metadata only and never mutates `job.json`; title ≤ 120 chars, project ≤ 80 chars, control chars rejected, archived strict bool, empty strings clear fields, entries with no remaining fields pruned on write; missing/malformed sidecar degrades silently with one-line log reason; new `GET /api/library` returns project rollups + counts (`{total, active, archived}`) keyed only to jobs that exist on disk; `GET /api/jobs` default-excludes archived and accepts `?include_archived=1`; every job summary gains `display_title` (custom title → original_filename → job_id), `custom_title`, `project`, `archived` plus `urls.metadata` + `urls.library`; new `POST /api/jobs/:id/metadata` accepts partial `{title?, project?, archived?}` updates behind Host pinning + `X-Recap-Token` CSRF + `_API_POST_BODY_MAX` body cap and returns the refreshed summary; React `/app/` jobs index gains Active / Archived view tabs, project dropdown filter, inline Edit / Archive / Unarchive affordances per `JobCard`; React `/app/job/:id` hero gains a Rename / Project panel and an Archive / Unarchive text link; archived jobs are still reachable by direct URL; `scripts/verify_api.py` grows by 17 cases for 102 total; Vitest adds `JobLibrary` tests covering custom title / project chip / archived badge, inline editor POST, error surface, and JobsIndex project filter + archive view toggle for 79 total; no new runtime deps; `recap run` composition and `recap/job.py STAGES` unchanged; legacy HTML routes unchanged)
- [ ] Delete jobs from the UI (separate slice; archive covers the non-destructive half)
- [ ] Live status updates (SSE / WebSocket / polling)
- [ ] Auth / API key management surface
- [ ] Remote access (non-localhost binding with TLS and auth)

## Modern web app

- [x] Add JSON API scaffold and Vite/React transcript workspace at `/app/job/<id>/transcript` (React 18 + Vite + TypeScript + Vitest; Python serves `web/dist` under `/app/*`; API endpoints cover CSRF, job summary, transcript JSON, and speaker-name overlays; speaker names persist in `speaker_names.json`; `transcript.json` and exporters unchanged; old HTML routes remain live)
- [x] React jobs index page (`/app/`) backed by `/api/jobs` (malformed `job.json` entries skipped server-side; `AppShell` sticky top bar links to legacy dashboard and `/new`; `JobCard` shows status badge + artifact chips + formatted created/updated times + actions; client-side search by filename/job_id and status pill filter; `verify_api.py` extended to 14 checks covering the listing and malformed-entry skip; Vitest specs for `JobCard` and `JobsIndexPage` raise total to 7; HTML jobs index at `/` remains live)
- [x] Polish React UI and transcript interactions (visual-system rewrite in `web/src/index.css` with CSS custom-property tokens, typography scale, elevation, radii, focus ring, and `prefers-reduced-motion` guard; jobs index hero + stats row; `JobCard` status stripe + artifact chips + action hierarchy; sticky left rail in transcript workspace; client-side transcript search with match count + prev/next + highlight rendering + Enter/Shift+Enter cycling + scroll active match into view, backed by `lib/search.ts`; speaker-filter chips on the speaker legend with `aria-pressed` toggling and "Show all" reset; cleaner empty/loading/error states; Vitest suite grows to 22 specs across six files)
- [x] Structured insights + report integration (new `recap insights --job <path> --provider mock|groq` stage writes `insights.json` with overview / quick bullets / per-chapter summaries / action items; mock provider is deterministic and offline, Groq provider uses stdlib HTTP with `GROQ_API_KEY`/`GROQ_MODEL` and fails cleanly on a missing key; `recap assemble`, `recap export-html`, and `recap export-docx` render `## Overview` and enrich chapter headings when `insights.json` is present and stay byte-compatible when it is absent; UI whitelists `insights.json`, API surfaces the `insights_json` artifact flag and URL, React `JobCard` shows an "Insights" chip; `scripts/verify_reports.py` grows to 23 checks (4 new insights cases), `scripts/verify_api.py` grows to 15 checks; `job.STAGES` and `recap run` composition unchanged; no new Python deps; [docs/product_roadmap.md](docs/product_roadmap.md) slice 3)
- [x] React job detail dashboard at `/app/job/:id` (new `JobDetailPage` with hero + status badge + created/updated + metadata chips derived from `job.stages.transcribe` and artifact flags; `StageTimeline` component rendering core Phase-1 stages plus any optional stages recorded in `job.json`, with per-stage status / start / finish / error / extras; `ArtifactGrid` component listing nine artifact tiles with open/download links when present and a "not generated yet" affordance otherwise; `InsightsPreview` component that fetches `/api/jobs/:id/insights`, renders the overview/bullets/action items/chapter count when present, shows a strong CLI-command empty state when absent, and a clear error state when malformed; `JobCard` primary click on `/app/` now opens the new dashboard with a secondary "Transcript" action; new `GET /api/jobs/:id/insights` read-only endpoint plus `react_detail` / `legacy_detail` / `insights` URL keys in job summaries; `scripts/verify_api.py` grows to 18 checks covering the new endpoint (404 absent, 200 present, 500 malformed) and the new summary URL keys; Vitest suite grows to 33 specs across nine files with coverage for `JobDetailPage` loading/success/missing-insights/error states, `StageTimeline` order + failed-status rendering, and `ArtifactGrid` present/missing logic; no changes to `recap/cli.py`, `recap/job.py STAGES`, `recap run` composition, `requirements.txt`, or `pyproject.toml`; legacy HTML detail page remains live; [docs/product_roadmap.md](docs/product_roadmap.md) slice 4)
- [x] Harden insights + align project guidance (`validate_insights` now requires + validates the top-level `sources` block; `_load_context` codifies per-artifact malformed-JSON policy — `speaker_names.json` and `selected_frames.json` degrade gracefully, `chapter_candidates.json` fails cleanly with the canonical `... malformed: ...` prefix; Groq request body carries a bounded `max_tokens = GROQ_DEFAULT_MAX_TOKENS (8_000)` on top of the client-side `MAX_RESPONSE_BYTES` cap; module docstring records the secrets-discipline invariant; `scripts/verify_reports.py` grows to 29 checks with six new regression cases (opt-in static guard, `sources` schema, malformed-speaker-names graceful, malformed-selected-frames graceful, malformed-chapter-candidates fail-clean, Groq max_tokens static check); AGENTS.md rewritten to drop the stale Phase-1-only language and point at `docs/product_roadmap.md` as the active roadmap while `MASTER_BRIEF.md` remains the pipeline philosophy; README / HANDOFF / CONTINUATION / docs reconciled for Deepgram + Groq priority, React `/app/` primary, legacy HTML routes retained as fallback)
- [x] React rich-report progress + insights/rich-report action endpoints (new `POST /api/jobs/<id>/runs/insights` + `GET .../last` and `POST /api/jobs/<id>/runs/rich-report` + `GET .../last`; insights handler runs `python -m recap insights --job <dir> --provider <p> [--force]` via subprocess boundary with provider allowlist `{mock, groq}` and Groq-requires-`GROQ_API_KEY` check; rich-report handler reuses legacy `_background_rich_report` + the shared `(job_id, "rich-report")` `_last_run` entry so React and legacy HTML share state; both POSTs reuse Host pinning, `X-Recap-Token` CSRF, 8 KiB body cap, global `_run_slot` semaphore, and per-job lock transferred to the worker; new React `RunActionsPanel` on `/app/job/:id` renders missing-vs-present insights CTA, provider select + force checkbox, 2.5 s status polling while in-flight, pending/running/completed/failed pill + colored dot (status is not color-only), ordered stage list with failed-stage stderr inlined as bounded `<pre>`, auto-refresh of job summary + insights preview on completion, and a fallback link to the legacy `/job/<id>/run/rich-report/last` page; `scripts/verify_api.py` grows by 12 cases covering no-run status, missing CSRF, invalid provider, Groq-without-key, no-such-job, stub dispatch + post-stub status for both run types and the 11-stage chain order; Vitest `RunActionsPanel` suite covers the seven interaction states; test-only `RECAP_API_STUB_RUN=1` env flag, opted into only by `scripts/verify_api.py`, skips the real subprocess and writes a canned success entry synchronously so CI never spawns `recap insights` or the real chain; no changes to `_RUNNABLE_STAGES`/`_LAST_RESULT_STAGES`, `recap/job.py STAGES`, `recap run` composition, legacy `/new`, `POST /run`, legacy rich-report routes, or exporter rerun routes; no new Python runtime deps; [docs/product_roadmap.md](docs/product_roadmap.md) slice 4b)
- [x] React new-job page at `/app/new` with engine selector (new `GET /api/sources` lists supported videos under `--sources-root` with `name`/`size_bytes`/`modified_at`; new `GET /api/engines` reports `faster-whisper` always + `deepgram` only when `DEEPGRAM_API_KEY` is set, without echoing the key; new `POST /api/jobs/start` accepts `{source:{kind:"sources-root"|"absolute-path", ...}, engine}`, reuses the legacy `POST /run` safety primitives — Host pinning, `X-Recap-Token` CSRF, 8 KiB body cap, source-path containment under the sources root, engine allowlist, `_run_slot` semaphore — and the shared ingest + background-run implementation, returning 202 with `{job_id, engine, react_detail, legacy_detail, started_at}`; new `NewJobPage` renders source picker + absolute-path fallback + engine selector with keyboard-accessible controls, visible focus, empty state, loading/error/success states, "what happens next" panel, and a fallback link to `/new`; `AppShell` "New job" CTA now points at `/app/new`; `scripts/verify_api.py` grows to cover `/api/sources`, `/api/engines`, `/api/jobs/start` with invalid CSRF / invalid engine / Deepgram-without-key / outside-sources-root / traversal-name / valid-dispatch cases, using the test-only `RECAP_API_STUB_JOB_START=1` shim so CI never runs real transcription; Vitest suite grows with `NewJobPage` specs for loading, sources render + selection, Deepgram-disabled, successful start + redirect, and error state; no new deps; `recap/job.py STAGES` and `recap run` composition unchanged; legacy `/new` + `POST /run` remain live as fallbacks; browser screen recording remains a future slice — no file upload)
- [ ] Exporter integration: assemble / export-html / export-docx read `speaker_names.json`
- [x] Chapter sidebar + editable chapter titles (new `chapter_titles.json` overlay with atomic `<file>.tmp` → `os.replace` write, 120-char cap, integer-string keys, empty-value-clears policy, graceful malformed-overlay fallback, `JobPaths.chapter_titles_json` property; new `GET /api/jobs/:id/chapters` merged view combining `chapter_candidates.json` + `insights.json` + overlay with `display_title = custom_title || fallback_title` and `fallback_title` preferring insights title → first sentence of candidate text → `"Chapter N"`; new `GET /api/jobs/:id/chapter-titles` + `POST /api/jobs/:id/chapter-titles` reusing Host pinning, `X-Recap-Token` CSRF, `_API_POST_BODY_MAX` body cap, per-job lock, and `speaker_names.json`-style sanitization; new React `ChapterSidebar` in the transcript workspace left rail — click-to-seek, active-chapter highlight tied to `timeupdate`/`seeking`/`play` with `aria-current` not color-only, inline rename with Enter-saves / Escape-cancels / empty-clears, `custom` pill when overlay is active, optional summary + bullets + action items from insights, empty-state with CLI command hints; new React `ChaptersCard` preview on `/app/job/:id` with first-five titles, custom-title count, and link to transcript workspace; `chapter_titles.json` added to `_JOB_ROOT_FILES` static whitelist so the raw file serves at `/job/:id/chapter_titles.json`; `RunActionsPanel` completion callback refreshes the chapter list so a freshly-run insights / rich-report shows up without reload; `scripts/verify_api.py` grows by 13 cases (empty baseline, candidates-only, insights-only, merged with custom titles, missing CSRF, forged Host, bad-key-shape, too-long, control-char rejection, success + round-trip, empty-clears, malformed-overlay graceful) for 58 total; Vitest adds `ChapterSidebar` tests (empty state, active-state aria + custom pill, onSeek callback, rename flow posting trimmed value, save-error inline render, Escape cancel) + `saveChapterTitles` unit test with CSRF-header assertion for 59 total; `recap/job.py STAGES` unchanged; `recap run` composition unchanged; no new runtime deps; legacy HTML routes + `POST /run` unchanged; exporter integration — `recap assemble` / `export-html` / `export-docx` honoring the overlay — is tracked as a follow-up below)
- [x] Screenshot / frame review UI (new `frame_review.json` overlay with atomic `<file>.tmp` → `os.replace` write, `JobPaths.frame_review_json` property; keys must pass `is_safe_frame_file` **and** carry a whitelisted image extension; values `{decision ∈ {keep, reject}, note ≤ 300 chars}`; `decision="unset"` removes the mapping; malformed overlay gracefully falls back to empty; new `GET /api/jobs/:id/frames` merges `candidate_frames/` + `frame_scores.json` + `scenes.json` + `selected_frames.json` + the overlay into a normalized list with `image_url`, `timestamp_seconds`, `chapter_index`, algorithm `decision`/`shortlist_decision`/`rank`/`composite_score`/`clip_similarity`/`text_novelty`/`ocr_text`/`verification`/`reasons`, and `review.{decision, note}` — always 200 with empty arrays when no artifacts exist, malformed upstream artifacts log short reason and are skipped; new `GET /api/jobs/:id/frame-review` + `POST /api/jobs/:id/frame-review` reusing Host pinning, `X-Recap-Token` CSRF, `_API_POST_BODY_MAX` body cap, per-job lock, speaker-names-style sanitization; new `/app/job/:id/frames` React route with `FrameReviewPage` + `FrameCard`; dashboard hero adds a **Review screenshots** link that flips copy when `selected_frames.json` is missing; page layout is a responsive 320px-minmax grid of cards showing image + timestamp + chapter context + algorithm pills (hero/supporting/rejected, shortlist decision, rank) + composite/CLIP/text-novelty/VLM metadata + optional caption + collapsible OCR, with a `keep`/`reject`/`unset` fieldset + 300-char note; changes batch locally with a dirty count, single **Save review** POST, **Discard changes** revert, and filter tabs (All/Shortlist/Selected/Reviewed); empty-state explains rich report command; `frame_review.json` added to `_JOB_ROOT_FILES` whitelist so it serves at `/job/:id/frame_review.json`; `scripts/verify_api.py` grows by 13 cases (candidates-only, enriched-with-selected, empty-overlay, missing CSRF, traversal filename, disallowed extension, bad decision, too-long note, control-char note, success + round-trip, overlay merged back into `/frames`, `unset` removes entry, malformed-overlay graceful) for 71 total; Vitest adds `FrameReviewPage` tests (empty state, grid rendering, save dispatch with CSRF header + dirty-count + success toast, bounded error on save, Reviewed filter) for 64 total; no changes to `recap/stages/*`; `recap/job.py STAGES` unchanged; `recap run` composition unchanged; no new runtime deps; legacy HTML routes unchanged; exporter integration reading `frame_review.json` is tracked as a follow-up below)
- [x] React product polish pass (frontend-only consolidation, 2026-04-21: tighter `JobCard` with one primary CTA + readiness sentence; `JobsIndexPage` flattened stats and "New recording" CTA; `JobDetailPage` hero collapsed to single subline + ordered action strip + `<details>` artifact disclosure; `RunActionsPanel` dropped verbose intro and folded "11-stage chain" notes into an "About this run" disclosure; `TranscriptWorkspacePage` removed redundant artifact summary and tightened header; `FrameReviewPage` groups frames by chapter with editorial `Chapter N — display_title` headings and hides per-frame raw scoring behind a "Scoring details" disclosure; `AppShell` simplified to one primary CTA + quieter `Legacy` link; body background reduced to a single soft warm gradient; no API or backend changes; legacy HTML routes unchanged; no runtime deps added; all 64 Vitest specs and the full `verify_reports`/`verify_ui`/`verify_api` suite remain green)
- [x] Refine React UI from screenshot audit (2026-04-21 follow-up to the premium redesign pass; drives visual changes from real Playwright screenshots at 1440 × 900 and 390 × 844 instead of theory. `StageTimeline` right rail rewritten so each stage renders name + status + finish-time only and the previous `extras` dictionary dump moved behind a per-stage `Details N ▾` disclosure; compact `N / M done · F failed` header replaces the "N stage(s)" label. `JobCard` readiness sentence replaced by three pill dots `T / R / I` (filled when `transcript_json` / any `report_*` / `insights_json` is present), wrapper carries an `aria-label` + visually-hidden state sentence so nothing is color-only; library grid stepped up to `minmax(440 px, 1fr)` so cards breathe. Detail-page hero demotes report links to a dedicated `Downloads HTML · Markdown · DOCX` row below the primary strip and moves `Legacy detail page` into the meta-actions row so the primary CTA stands alone. `ChapterSidebar` per-row summary / bullets / action_items collapsed behind an `Outline ▾` disclosure, keeping the rail as a clean nav list. `FrameCard` visuals tightened via CSS only — segmented-pill Keep/Reject/Unset control, 16:9 image aspect, compact headline/caption/fieldset — DOM structure unchanged so `getByLabelText("Keep")`, `getByLabelText("Reject")`, and placeholder `/why keep or reject/i` tests stay green. `source-root-chip` dropped its 60 % cap so long absolute paths wrap cleanly. One small scoped block in `web/src/index.css` reusing existing tokens — no new palette, shadow, or elevation tokens. Audit findings + before/after screenshot paths recorded in [CONTINUATION.md](CONTINUATION.md); `docs/ux_inspiration.md` cross-references. All 79 Vitest specs + the full `verify_reports` / `verify_ui` / `verify_api` suite remain green. No new runtime deps; `recap/job.py STAGES` and `cmd_run` composition unchanged; legacy HTML routes unchanged.)
- [x] Premium React UI redesign pass (2026-04-21 follow-up; `web/src/index.css` grows with a cohesive token override section at the bottom: calmer warm palette and flatter background, tighter radii, softer single-layer shadows, brand-tinted 3 px focus ring, ink-strong primary button instead of warm gradient, neutral `.detail-chip` for metadata with colored badges reserved for status only, 3 px left-edge accent stripe for status on `JobCard` without color-only reliance, segmented `.mode-toggle` pill on `/app/new`, collapsed "What happens next" into a `<details>` disclosure, consolidated detail-page hero into a primary action strip + quieter organize strip with a labeled report chip group, detail-page artifacts into a `<details>` disclosure below insights/chapters/run actions, transcript-workspace metadata as chip row instead of run-on sentence, frame-review header chips for totals, responsive collapses at 960/640 px so no page scrolls horizontally; no API or backend changes; no new runtime deps; `recap/job.py STAGES` and `cmd_run` composition unchanged; all 79 Vitest specs and the full `verify_reports`/`verify_ui`/`verify_api` suite remain green; [docs/ux_inspiration.md](docs/ux_inspiration.md) records the concrete Cap5 / CapSoftware / steipete/summarize / Tabler / Nord patterns borrowed)
- [x] Exporters honor overlays fully (`recap assemble` / `export-html` / `export-docx` now read `speaker_names.json`, `chapter_titles.json`, and `frame_review.json` at render time; new helpers in `recap/stages/report_helpers.py`: `load_speaker_names_overlay`, `load_chapter_titles_overlay`, `load_frame_review_overlay`, `resolve_chapter_title`, `resolve_speaker_label`, `apply_frame_review_to_chapter`, `iter_transcript_utterances`; precedence is `chapter_titles > insights > Chapter N` for headings, `frame_review reject` suppresses the hero and supporting frames, `frame_review keep` promotes a `vlm_rejected` frame into the supporting list sorted by `scene_index`, `speaker_names` substitutes the `Speaker N` prefix when the transcript carries `utterances[]` with valid speaker ids while segments-only transcripts stay byte-compatible; coherence checks on `selected_frames.json` / `chapter_candidates.json` still run before the overlay is applied so corrupted upstream artifacts keep failing with the canonical `... malformed: ...` prefixes; missing or malformed overlay JSON degrades to empty overlay with no behavior and byte-identical output; no runtime deps added; `recap run` composition unchanged; `recap/job.py STAGES` unchanged; `recap/stages/scenes.py` / `dedupe.py` / `window.py` / `similarity.py` / `chapters.py` / `rank.py` / `shortlist.py` / `verify.py` untouched; `scripts/verify_reports.py` grows from 29 → 48 checks covering empty-overlay byte-compat, chapter_titles MD/HTML/DOCX, chapter_titles beats insights, frame_review reject removes hero MD/HTML/DOCX, frame_review keep promotes MD/HTML/DOCX, speaker_names MD/HTML/DOCX, partial speaker overlay fallback, malformed-overlay graceful-fallback, and frame_review reject wins over selected_hero)
- [ ] Selected-speaker playback ranges (play only ranges where a chosen voice speaks)
- [x] Transcript correction / notes overlay (new `transcript_notes.json` overlay with atomic `<file>.tmp` → `os.replace` write, `JobPaths.transcript_notes_json` property; keys `^(utt|seg)-\d+$` mirror React row ids emitted by `buildTranscriptRows` which now produces `utt-<n>` / `seg-<n>`; corrections trimmed and capped at 2000 chars; notes trimmed and capped at 1000 chars; control chars other than tab/newline rejected (line breaks are allowed for freeform text); empty field clears just that field, empty item drops the mapping, merge-on-server so single-row saves don't blow away other rows; dedicated 64 KiB body cap; new `GET /api/jobs/:id/transcript-notes` + `POST /api/jobs/:id/transcript-notes` reusing Host pinning, `X-Recap-Token` CSRF, per-job lock, speaker-names-style sanitization; `transcript_notes.json` added to `_JOB_ROOT_FILES` whitelist so it serves at `/job/:id/transcript_notes.json`; job-summary `urls` expose `transcript_notes` + `transcript_notes_json`; artifacts map carries `transcript_notes_json` flag; new React `TranscriptNotesEditor` component with correction + note textareas, canonical-text preview, character counters, Ctrl/Cmd+Enter save, Escape cancel; `TranscriptTable` grows a per-row **Note** button that appears on hover/focus/when an overlay entry exists, inline editor slot below the row via `renderRowEditor`, `edited` / `note` badges, note-preview paragraph, and a `Show corrections` toggle in the card header; `JobDetailPage` meta line adds a `Transcript notes` chip when the artifact is present; canonical `transcript.json` is never mutated; `scripts/verify_api.py` grows by 14 cases (empty baseline, missing CSRF, traversal key, no-prefix key, correction-too-long, note-too-long, control chars, success + roundtrip, empty field clears just field, empty item clears mapping, upstream transcript bytes unchanged, malformed overlay graceful, raw-artifact static serving) for 85 total; Vitest adds 9 new checks across `TranscriptNotesEditor` and the row-affordance flow for 73 total; no changes to `recap/stages/*`; `recap/job.py STAGES` unchanged; `recap run` composition unchanged; no new runtime deps; legacy HTML routes unchanged; exporter integration reading `transcript_notes.json` is tracked as a follow-up below)
- [x] Exporters honor `transcript_notes.json` overlay (`recap assemble` / `export-html` / `export-docx` now read `transcript_notes.json` at render time via new helpers in `recap/stages/report_helpers.py`: `load_transcript_notes_overlay`, `transcript_row_id`, `resolve_transcript_row`, plus a shared `_overlay_contains_line_control` that allows tab/newline/CR for reviewer prose; the exporters walk `utterances[]` (when present with valid speaker ids) or `segments[]` by positional index, look up each row by `utt-<n>` / `seg-<n>`, and — when the overlay entry has a `correction` — swap in the corrected text followed by a `*(edited)*` / `(edited)` marker so downstream readers still see that the row was user-corrected; rows with a `note` emit an italic `Note:` block subordinate to the transcript row (indented `_Note:_` bullet in Markdown, `<p class="transcript-note">` in HTML with a small inline-CSS rule, italic indented paragraph in DOCX with newlines becoming paragraph breaks); missing / malformed / empty overlays degrade to the no-overlay baseline byte-for-byte; `transcript.json` is never mutated; the three `run()` functions record `overlays.transcript_notes` in the job-state `extra` dict; `scripts/verify_reports.py` grows by 12 cases (segment correction + note across MD/HTML/DOCX with upstream transcript bytes-unchanged assertion, note-only preserves canonical, utterance correction via speaker label in all three exporters, malformed-overlay graceful, empty-overlay byte-compat) for 60 total; no new runtime deps; `recap/job.py STAGES` unchanged; `recap run` composition unchanged; `recap/stages/scenes.py` / `dedupe.py` / `window.py` / `similarity.py` / `chapters.py` / `rank.py` / `shortlist.py` / `verify.py` / `recap/ui.py` untouched; legacy HTML routes unchanged)
- [ ] Transcript text editing / further correction surfaces (word-level edits, diff display, speaker-level bulk edits; future slice)
- [ ] Playwright browser coverage for the React workspace
- [ ] Linux deployment docs and systemd unit sample

## Reliability

- [x] Mark `recap scenes` as `failed` (not `running`) when Ctrl-C interrupts PySceneDetect/cv2 frame processing; clean up partial `candidate_frames/` for the interrupted attempt; re-raise `KeyboardInterrupt` so the CLI still exits like an interrupted command
- [x] Harden `recap normalize` and add an MP4 fast path (`recap/stages/normalize.py` rewritten: every output is written to `<target>.tmp` and promoted with `os.replace` only after the FFmpeg subprocess exits cleanly **and** a shape-check `ffprobe` confirms the tmp is readable, so a hung/killed/corrupt run can never leave a bad `analysis.mp4` at the final path; new `_decide_normalize_mode(probe)` picks `"remux"` for MP4/MOV containers with H.264 + yuv420p video and AAC-or-absent audio, falls back to `"reencode"` otherwise; remux path uses `-c copy -movflags +faststart` so a compatible 56-minute MP4 finishes in seconds instead of re-encoding; new `_run_ffmpeg_streaming` runs FFmpeg under a stderr-drain thread, samples output-file size + stderr activity, kills and fails cleanly if neither moves for `RECAP_NORMALIZE_STALL` seconds (default 90), and enforces a wall-clock `RECAP_NORMALIZE_TIMEOUT` cap (default 2h); heartbeats every ~2s call `update_stage(paths, "normalize", RUNNING, extra={command_mode, elapsed_seconds, output_bytes, phase, percent, input_duration_seconds})` so `job.json#updated_at` advances while normalize is running; env escape `RECAP_NORMALIZE_NO_FASTPATH=1` forces full re-encode; failures unlink every `*.tmp` and mark the stage `failed` with a concise one-line error; `scripts/verify_reports.py` grows by four regression checks (pure-function mode decision across MP4/MOV/HEVC/Opus/yuv444p/WebM cases + `RECAP_NORMALIZE_NO_FASTPATH` override; monkey-patched stall-failure cleans every tmp and marks `failed`; monkey-patched bad-tmp rejects promotion; static pin that `STAGES` and `cmd_run` composition are unchanged) for 64 total; no new runtime deps; `recap/job.py STAGES` and `recap/cli.py cmd_run` composition unchanged)

## Hardening

- [x] Add local offline golden-path validation script for Markdown/HTML/DOCX report generation (`scripts/verify_reports.py` + committed fixture under `scripts/fixtures/minimal_job/`; runs `recap assemble`, `recap export-html`, `recap export-docx` against both the selected and absent-selected paths plus negative cases for malformed `selected_frames.json`, traversal `frame_file`, and missing candidate images; stdlib + `python-docx` only; no network, no model downloads; ~1 second wall-clock)
- [x] Add local offline UI smoke validation script (`scripts/verify_ui.py`; spawns `recap ui` against a temp copy of `scripts/fixtures/minimal_job`, checks `/`, `/job/<id>/`, whitelisted JSON artifacts, candidate-frame JPEG, unnormalized traversal path via `http.client` for raw-path delivery, and non-whitelisted/unknown routes; then runs `recap assemble`/`export-html`/`export-docx` and re-checks that `report.html`, `report.docx`, and the referenced `candidate_frames/<file>.jpg` all serve correctly; stdlib only; ~0.5 second wall-clock)
- [x] Deduplicate shared selected-frame / report helpers across the three report stages into `recap/stages/report_helpers.py` (no-behavior-change refactor; `recap/stages/assemble.py`, `recap/stages/export_html.py`, and `recap/stages/export_docx.py` now import `format_ts`, `summarize_metadata`, `collapse_whitespace`, `is_int`/`is_number`/`is_safe_frame_file`, `validate_selected_frames`, `validate_chapter_candidates`, `caption_for`, `check_hero_coherence`, and `check_supporting_coherence` from the shared module; error-message prefixes preserved; `scripts/verify_reports.py` + `scripts/verify_ui.py` both green; no-selected Markdown and selected Markdown+HTML outputs are byte-identical pre-refactor vs post-refactor)

## Guardrails

- [ ] Do not add fixed-interval screenshot capture anywhere in the pipeline
- [ ] Do not send full raw videos to a VLM
- [ ] Do not make VLM usage mandatory for successful processing
- [ ] Do not expand Phase 1 beyond the reliable core
